Code-built GUI lives here: the Ui builder and one ModuleScript per screen
(jobs C1, B3, B5) — see ARCHITECTURE §2.

This file exists because git does not track empty directories, and
default.project.json maps this path. Without it a fresh clone fails to build.
