--!strict
--[[
	RETIRED from the corrected M1 slice by D-017.

	All players use one shared hunting wilderness. Future hunting-region
	progression requires a new approved packet; this module intentionally
	registers no unlock or teleport remotes.
]]

local ZoneService = {}

function ZoneService.Init() end
function ZoneService.Start() end

return ZoneService
